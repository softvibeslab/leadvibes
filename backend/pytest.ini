[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q -m "not integration"
markers =
    integration: Tests que llaman API remota con requests (requieren REACT_APP_BACKEND_URL y datos seed).
